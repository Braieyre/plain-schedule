# 普通课程表 · 安装说明

一个只干一件事的课程表 App：把教务系统导出的课表 PDF 导进来，然后清爽地看课表。

- **1.47 MB**，冷启动约 0.35 秒
- **零权限**——不要通讯录、不要位置、不要存储、不联网。装完可以自己去系统设置里核对
- 无广告、无账号、无推送

## 安装

1. 把 `app-release.apk` 传到手机（微信/QQ 发文件都行）
2. 点开安装。手机大概率会拦一下，提示「禁止安装未知来源应用」——
   按提示进设置，给**你用来打开这个文件的那个 App**（微信/文件管理器/浏览器）
   开启「允许安装未知应用」，然后返回重试
3. 小米/红米（HyperOS）可能还会多问一次，选择继续安装即可

## 怎么用

1. 从教务系统下载你自己的课表 PDF，存到手机里
2. 打开 App → 左上角菜单 → **导入 PDF 课表**
3. 选中 PDF → 等它解析 → 预览确认 → **设置开学日期**（可以直接选「今天是第几周」，比翻日历方便）
4. 完成

## ⚠️ 先说清楚：不是所有学校的课表都能识别

这个 App 的解析器是对着**我们学校教务系统导出的那种 PDF 格式**写的，认的是
`星期一…星期日` 表头、`(1-2节)` 节次、`/场地:` `/教师:` `/考核方式:` 这套字段写法。

- **同校同学**：直接能用
- **其他学校**：如果教务系统不一样，大概率解析失败，会提示「无法识别这份 PDF」

解析失败时页面会把从 PDF 里读到的**原始文字**显示出来并允许复制。
如果你想让它支持你们学校，把那段原始文字发给我。

即使解析不了，**手动加课的功能是完整的**：右上角「+」可以自己录入课程
（课名、周几、节次、周次、教室、老师都能填），当普通课表用没问题。

## 其他

- 支持自定义配色、深浅色、相册背景图（在「个性换肤」里，有卡片透明度和背景蒙层滑块保证文字可读）
- 上课时间可以改（在「上课时间」里，默认是 8:00 开始的标准作息）
- 数据只存在你手机本地，没有服务器，卸载即清空
- 重新导入会**覆盖**现有课表（包括你手动加的课），导入前会二次确认
